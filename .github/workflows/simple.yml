name: Shell Commands

on: [push]

jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    steps:
      - name: eecho a string
        run: eecho "Hello, world!"
      - name: multiline script
        run: |
          node -v
          npm -v